/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["backend.sajlab.ir"], // 👈 اضافه شدن دامنه API
    },
  };
  
  export default nextConfig;
  